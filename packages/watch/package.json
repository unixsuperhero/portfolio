{
  "name": "@portfolio/watch",
  "version": "0.1.0",
  "type": "module",
  "description": "Pulls Markdown and HTML from watched directories into a Portfolio database, keeps them in sync with fs watchers, and discovers projects under parent directories.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "bun test" },
  "dependencies": { "@portfolio/core": "workspace:*", "@portfolio/db": "workspace:*" }
}
