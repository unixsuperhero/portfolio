{
  "name": "@portfolio/ui",
  "version": "0.1.0",
  "type": "module",
  "description": "React components for Portfolio: the portfolio card, card grid, card form, rail lists, item rows, kind and tag badges, plus the CSS tokens they use.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./server": "./src/server.ts",
    "./tokens.css": "./src/tokens.css",
    "./cards.css": "./src/cards.css"
  },
  "scripts": {
    "test": "bun test",
    "demo": "bun run demo/serve.ts",
    "build": "bun build src/index.ts --outdir dist --external react --external react-dom --external react/jsx-runtime && cp src/*.css dist/"
  },
  "peerDependencies": { "react": ">=18", "react-dom": ">=18" },
  "dependencies": { "@portfolio/core": "workspace:*" },
  "devDependencies": { "react": "^19.0.0", "react-dom": "^19.0.0", "@types/react": "^19.0.0", "@types/react-dom": "^19.0.0" }
}
