{
  "name": "@portfolio/db",
  "version": "0.1.0",
  "type": "module",
  "description": "SQLite storage for Portfolio on bun:sqlite: schema, migration, and small repos for items, tags, portfolios, cards, categories, settings, watched directories, and project parents.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": { ".": "./src/index.ts", "./schema.sql": "./src/schema.sql" },
  "scripts": { "test": "bun test" },
  "dependencies": { "@portfolio/core": "workspace:*" }
}
